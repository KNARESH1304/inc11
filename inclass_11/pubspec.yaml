name: inclass_11
description: "A beautiful card organizer app with SQLite local storage"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.9.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.3.0
  path: ^1.8.3
  cached_network_image: ^3.3.0
  flutter_slidable: ^3.0.0
  cupertino_icons: ^1.0.8
  http: ^1.1.0  # Added this for HTTP requests

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true